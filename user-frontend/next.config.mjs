/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['dnwbvytj9ltty.cloudfront.net'], // ✅ Add your CloudFront domain
    },
  };
  
  export default nextConfig;
  